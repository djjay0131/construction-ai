"""Object catalog package.

Submodules:

* ``spatial_association`` — assigns OCR dimensions to architectural elements
* ``catalog_builder`` — constructs a Catalog from raster pipeline outputs
* ``catalog_store`` — JSON-based persistence (round-trip-safe)
"""
